@model IEnumerable<Entity.TipoInmobiliario>

@{
    ViewBag.Title = "ListarTipoInm";
}

<h2>ListarTipoInm</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NombreTipoInmobiliario)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NombreTipoInmobiliario)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TipoInmobiliarioId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TipoInmobiliarioId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TipoInmobiliarioId })
        </td>
    </tr>
}

</table>
